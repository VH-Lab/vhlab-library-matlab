function [thresholds] = vhwillow_getthresholds(dirname)
%  VHWILLOW_GETTHRESHOLDS - Return threshold information for a directory
%
%  


   % should check the directory; if it has one, it should use it,
   % if not, it should check 
