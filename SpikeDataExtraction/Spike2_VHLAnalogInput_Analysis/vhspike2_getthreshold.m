function [thresholds] = vhspike2_getthresholds(dirname)
%  VHSPIKE2_GETTHRESHOLDS - Return threshold information for a directory
%
%  


   % should check the directory; if it has one, it should use it,
   % if not, it should check 
